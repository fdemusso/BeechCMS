{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8789",
    "build": "tsc -p tsconfig.build.json --noEmit",
    "db:migrate:local:seed-only": "wrangler d1 execute beech-db --local --file=./migrations/0001_init.sql && wrangler d1 execute beech-db --local --file=./migrations/0002_seed_admin.sql && wrangler d1 execute beech-db --local --file=./migrations/0003_refresh_tokens.sql && wrangler d1 execute beech-db --local --file=./migrations/0006_add_slug_and_unique_index.sql && wrangler d1 execute beech-db --local --file=./migrations/0008_seed_content_entries.sql && wrangler d1 execute beech-db --local --file=./migrations/0009_public_idempotency.sql",
    "db:reset:local:seed-only": "rm -rf .wrangler/state && npm run db:migrate:local:seed-only",
    "db:migrate:local": "wrangler d1 execute beech-db --local --file=./migrations/0001_init.sql && wrangler d1 execute beech-db --local --file=./migrations/0002_seed_admin.sql && wrangler d1 execute beech-db --local --file=./migrations/0003_refresh_tokens.sql && wrangler d1 execute beech-db --local --file=./migrations/0004_seed_projects.sql && wrangler d1 execute beech-db --local --file=./migrations/0005_update_projects.sql && wrangler d1 execute beech-db --local --file=./migrations/0006_add_slug_and_unique_index.sql && wrangler d1 execute beech-db --local --file=./migrations/0007_cleanup_test_data.sql && wrangler d1 execute beech-db --local --file=./migrations/0008_seed_content_entries.sql && wrangler d1 execute beech-db --local --file=./migrations/0009_public_idempotency.sql",
    "db:reset:local": "rm -rf .wrangler/state && npm run db:migrate:local",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.995.0",
    "@beech/core": "^0.0.0",
    "bcryptjs": "^2.4.3",
    "hono": "^4.11.9",
    "jose": "^6.1.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260213.0",
    "@types/bcryptjs": "^2.4.6",
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "^3.2.4",
    "wrangler": "^4.4.0"
  }
}
